spring:
  threads:
    virtual:
      enabled: true
  application:
    name: aggregator

server:
  port: 8093

services:
  user:
    base-url: http://localhost:8090/api/v1/users/
  loyalty:
    base-url: http://localhost:8092/api/v1/loyalties/
  order:
    base-url: http://localhost:8091/api/v1/orders/
